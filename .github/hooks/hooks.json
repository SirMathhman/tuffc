{
  "hooks": {
    "Stop": [
      {
        "type": "command",
        "command": "bun test; if ($LASTEXITCODE -ne 0) { exit 2 }"
      },
      {
        "type": "command",
        "command": "bun run lint; if ($LASTEXITCODE -ne 0) { exit 2 }"
      },
      {
        "type": "command",
        "command": "bun run find-duplicates; if ($LASTEXITCODE -ne 0) { exit 2 }"
      }
    ]
  }
}
