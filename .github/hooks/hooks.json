{
  "hooks": {
    "Stop": [
      {
        "type": "command",
        "command": "bun test; if ($LASTEXITCODE -ne 0) { exit 2 }"
      },
      {
        "type": "command",
        "command": "npm run find-duplicates; if ($LASTEXITCODE -ne 0) { exit 2 }"
      }
    ]
  }
}
