{
  "hooks": {
    "Stop": [
      {
        "type": "command",
        "command": "npm run test; if ($LASTEXITCODE -ne 0) { exit 2 }"
      },
      {
        "type": "command",
        "command": "npm run lint; if ($LASTEXITCODE -ne 0) { exit 2 }"
      },
      {
        "type": "command",
        "command": "npm run cpd; if ($LASTEXITCODE -ne 0) { exit 2}"
      }
    ]
  }
}
