{
  "hooks": {
    "Stop": [
      {
        "type": "command",
        "command": "bun test tests/compiler.compile.test.ts; if ($LASTEXITCODE -ne 0) { exit 2 }; bun test tests/compiler.execute.test.ts; if ($LASTEXITCODE -ne 0) { exit 2 }; bun test tests/placeholder.test.ts; if ($LASTEXITCODE -ne 0) { exit 2 }; node ./node_modules/eslint/bin/eslint.js --no-error-on-unmatched-pattern \"src/**/*.ts\" \"tests/**/*.ts\"; if ($LASTEXITCODE -ne 0) { exit 2 }; cmd /c pmd cpd --dir src,tests --language typescript --minimum-tokens 35 --ignore-identifiers --ignore-literals; if ($LASTEXITCODE -ne 0) { exit 2 }"
      }
    ]
  }
}
