{
  "hooks": {
    "Stop": [
      {
        "type": "command",
        "command": "make test; if ($LASTEXITCODE -ne 0) { exit 2 }"
      },
      {
        "type": "command",
        "command": "make find-duplicates; if ($LASTEXITCODE -ne 0) { exit 2 }"
      }
    ]
  }
}
