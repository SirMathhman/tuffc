{
  "hooks": {
    "Stop": [
      {
        "type": "command",
        "windows": "powershell -NoProfile -Command \"bun test --timeout 1000 --concurrent; if ($LASTEXITCODE -ne 0) { exit 2 }\"",
        "command": "bun test --timeout 1000 --concurrent",
        "timeout": 30
      }
    ]
  }
}
