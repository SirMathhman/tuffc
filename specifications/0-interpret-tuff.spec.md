# interpretTuff

## Goal
Provide a way to interpret Tuff input and return a result for empty input.

## User stories
- As a caller, I want `interpretTuff("")` to return `0` so that empty input is handled predictably.
- As a caller, I want the function to exist in the codebase so that I can invoke the interpreter from Rust code.
