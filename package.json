{
  "name": "tuffc",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "husky": "^9.1.7"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "find-duplicates": "pmd cpd index.ts --language typescript --minimum-tokens 35 --format markdown || exit 2",
    "prepare": "husky"
  }
}
