{
  "name": "tuffc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/index.js",
    "test": "bun test",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "cpd": "pmd cpd --dir src test --language ecmascript --minimum-tokens 35 --ignore-literals --ignore-identifiers 1>&2"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0"
  }
}
