{
  "name": "tuff",
  "version": "1.0.0",
  "description": "A TypeScript project using Bun",
  "main": "dist/index.js",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "start": "bun dist/index.js",
    "test": "bun test"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "latest",
    "@types/bun": "latest"
  }
}
