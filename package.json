{
  "name": "tuffc",
  "version": "0.1.0",
  "private": true,
  "description": "Simple TypeScript project scaffold",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "clean": "rimraf dist",
    "hooks:install": "git config core.hooksPath .github/githooks",
    "test": "vitest run",
    "test:watch": "vitest",
    "cpd": "pmd cpd --minimum-tokens 40 --language typescript --dir src --dir tests --ignore-literals --ignore-identifiers"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.0",
    "vitest": "^3.2.4"
  }
}
