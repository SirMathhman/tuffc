def interpret(text):
    """
    Accepts a string and returns the same string.

    Args:
        text (str): The input string

    Returns:
        str: The same input string
    """
    return text
