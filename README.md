# tuffc

A small Bun + TypeScript starter.

## Scripts

- `bun run dev` — run the app directly with Bun
- `bun run build` — type-check and emit JavaScript to `dist/`
- `bun run check` — run TypeScript in no-emit mode
- `bun run start` — run the built output from `dist/`
