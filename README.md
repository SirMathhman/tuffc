# tuffc

A minimal JavaScript project built with [Bun](https://bun.sh/).

## Scripts

- `bun run src/index.js` — run the app
- `bun test` — run the test suite

## Getting started

1. Install Bun.
2. Run `bun install` if you add dependencies later.
3. Use `bun run start` to execute the app.
