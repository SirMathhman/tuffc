# Tuffc

A small JavaScript project scaffolded for [Bun](https://bun.sh).

## Scripts

- `bun run src/index.js` — run the app
- `bun test` — run tests

## Setup

1. Install Bun.
2. Run `bun install` if you add dependencies later.
3. Run `bun test` to verify everything works.
