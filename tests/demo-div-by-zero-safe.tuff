// UNSAFE: Division by zero not proven impossible
// fn unsafe_divide(a : I32, b : I32) : I32 => a / b;

// SAFE: Guard against zero
fn safe_divide(a : I32, b : I32) : I32 => {
    if (b == 0) {
        0
    } else {
        a / b
    }
}

fn main() : I32 => safe_divide(10, 2);
