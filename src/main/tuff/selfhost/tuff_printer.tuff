let {
    emit_tuff_from_typed_impl, selfhost_tuff_printer_impl_marker
}
 = selfhost::internal::tuff_printer_impl;

// Phase 3 printer API: canonical .tuff emission entrypoint.
out fn emit_tuff_from_typed(typed: I32, source: *Str, trivia_payload: *Str) : *Str =>
    emit_tuff_from_typed_impl(typed, source, trivia_payload);

out fn selfhost_tuff_printer_marker() : I32 => selfhost_tuff_printer_impl_marker();
