struct DescriptiveError {
    source : *Str;
    description : *Str;
    reason : *Str;
    fix : *Str;
}

fn Wrapper(field : I32) : Wrapper => {
    fn get() => field;

    this
}

let temp : Wrapper = Wrapper(100);
temp.get()