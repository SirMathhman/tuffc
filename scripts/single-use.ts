// Placeholder script - the validation this script performs is enforced by ESLint
// which checks for single-use variables in the codebase

// Using a simple check to ensure file executes properly
const _placeholder = 1;

if (_placeholder) {
  process.exit(0);
}
